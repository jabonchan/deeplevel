export enum Endianness {
    System,
    Little,
    Big,
}

export enum TextEncoding {
    ASCII,
    UTF16,
    UTF32,
}

export enum PrimitiveTextEncoding {
    ASCII,
}

export enum WideTextEncoding {
    UTF16 = 1,
    UTF32,
}

export enum Alignment {
    Natural,
    Packed,
}
